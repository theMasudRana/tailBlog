const SectionTitle = ({ title }) => {
	return (
		<div className="section-title-wrapper">
			<h2 className="section-title text-2xl font-bold mb-5">{title}</h2>
		</div>
	);
};

export default SectionTitle;
